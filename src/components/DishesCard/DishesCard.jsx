import React from 'react'
import s from './DishesCard.module.css'

const DishesCard = (props) => {
	const { title, description, kind, photo, price } = props
	return (
		<div>root</div>
	)
}
export default DishesCard
